# Sample environment configuration for TrainStation deployment
# ⚠️  SECURITY WARNING: This is a sample file with example credentials.
# ⚠️  Copy this file to .env and replace ALL example values with secure credentials before production use!

POSTGRES_PASSWORD=brokentrack      # ⚠️ REPLACE WITH SECURE PASSWORD
POSTGRES_DB=trains
POSTGRES_USER=trainsAdmin
DATABASE_URL=postgresql+psycopg2://trainsAdmin:brokentrack@db:5432/trains  # ⚠️ UPDATE WITH SECURE PASSWORD
APP_ENV=prod
HW_MODE=mock